#include<stdio.h>
int main()
{
	int loop,scan;
	printf("Enter Number to print table: ");
	scanf("%d" , &scan);

		for(loop=1; loop<=10; loop++)
	{
		printf("%d * %d = %d\n" , num1, num2, (num1*num2));
    }
    getch();
}
